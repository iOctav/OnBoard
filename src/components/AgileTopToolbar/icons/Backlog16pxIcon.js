import styled from 'styled-components';

import { ReactComponent as BacklogIcon } from './backlog-icon.svg';

const Backlog16pxIcon = styled(BacklogIcon)`
  width: 16px;
  height: 16px;
  color: var(--ring-icon-secondary-color);
`;

export default Backlog16pxIcon;