import styled from 'styled-components';

import { ReactComponent as ChartIcon } from './chart-icon.svg';

const Chart16pxIcon = styled(ChartIcon)`
  width: 16px;
  height: 16px;
  color: var(--ring-icon-secondary-color);
`;

export default Chart16pxIcon;